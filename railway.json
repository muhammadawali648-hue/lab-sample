{
  "deploy": {
    "startCommand": "sh -c 'composer install --no-dev --optimize-autoloader --no-scripts && cp .env.example .env && php artisan serve --host=0.0.0.0 --port=$PORT'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
