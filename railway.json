{
  "deploy": {
    "startCommand": "sh -c 'composer install --no-dev --optimize-autoloader --no-scripts && cp .env.example .env && php artisan key:generate --force && php artisan config:clear && php artisan cache:clear && php artisan migrate --force || true && php artisan serve --host=0.0.0.0 --port=$PORT'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
